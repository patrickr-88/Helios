{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Helios needs only its own commands plus the save/open dialogs. No filesystem plugin, no shell plugin, no HTTP plugin: the app cannot write to or execute anything the user did not explicitly pick.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
